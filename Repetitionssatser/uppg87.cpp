#include <iostream>
using namespace std;

int main() {
	int n;
	int start;
	int summa =0;
	cin >> n;
	for(start=0; start <= n; n++){
		
		if (start != n) {
			
		cout << summa << '+';
		} else {
			summa = summa +n; 
			cout << n;
			}
			cout << 'i' << resultat
	}
	cout << start;
	return 0;
	}
